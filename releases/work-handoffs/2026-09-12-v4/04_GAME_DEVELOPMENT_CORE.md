# ONE-SHOT WORK HANDOFF — AI Game Development Core Integration

## Model routing
- MAIN orchestrator / canonical architecture: **GPT-6 Astra — XHigh**
- Architecture challenger/final integration judge: **GPT-6 Astra — XHigh**
- Core implementation workers: **GPT-5.6 Sol — High**
- Integration/regression/vertical-slice tests: **GPT-5.6 Terra — High**
- Repository scouting/inventory/comparison extraction: **GPT-5.6 Luna — Medium**
- Never use Pro in Work.

Take ownership of the larger AI game-production system. Do not build a separate permanent stack for every interesting repository.

Evaluate and selectively absorb the strongest capabilities from GameFactory-3A, Unity Agent Plugin, OpenAI game workflows/showcase, SoL-Pi, sprite-gen, Three.js game skills, and other already-investigated current agentic game-production systems. Recover existing architecture and implementation first.

For every candidate classify KEEP / ADAPT / REPLACE / REJECT / EXPERIMENTAL. Optimize for autonomous iteration, playable builds, visual verification, gameplay verification, asset generation/import, scene/world construction, animation, UI, debugging, performance, regression testing, checkpoints/rollback, and minimal duplicated orchestration.

Astra owns the canonical architecture and resolves conflicts. Sol implements selected missing capabilities. Terra forces subsystems to cooperate in real vertical-slice tests. Luna researches and inventories candidates but cannot make final adoption decisions.

Do not accept code-only success. The system must know whether a produced game actually launches and behaves correctly. Build the highest-value missing pieces and run a discriminating vertical slice.

Deliver canonical architecture, absorption/rejection matrix, implemented changes, real test evidence, and the remaining bottlenecks ranked by impact. Continue beyond planning.