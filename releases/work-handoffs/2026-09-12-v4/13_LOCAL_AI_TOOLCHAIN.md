# ONE-SHOT WORK HANDOFF — Kimaki / Oracle / Three.js / WebGPT Local Toolchain

## Model routing
- MAIN owner: **GPT-5.6 Sol — XHigh**
- Core integration/repair: **GPT-5.6 Sol — High**
- Architecture/final audit: **GPT-6 Astra — High**
- Regression/compatibility tests: **GPT-5.6 Terra — High**
- Dependency/version/log inventory: **GPT-5.6 Luna — Medium**
- No Pro in Work.

Consolidate the local AI development/tooling stack involving Kimaki, Oracle/WebGPT review workflows, Three.js game skills, and related local integrations. Recover current installs, wrappers, skills, prompts, versions, and prior failures first.

For each subsystem classify KEEP / UPDATE / REPLACE / REMOVE / MERGE. Optimize reliable invocation, context handoff, reproducible routing, explicit failures, version compatibility, local performance, minimal hidden state, maintainability, and tests. Do not keep a tool simply because it is interesting.

Sol owns integration and repairs. Terra executes real representative workflows and compatibility regressions. Luna inventories dependencies/logs. Astra reviews the final architecture for duplication and weak boundaries.

Eliminate fake-success wrappers and document canonical entry points so future agents do not create parallel stacks. Continue through actual integration and tests, not just advice.