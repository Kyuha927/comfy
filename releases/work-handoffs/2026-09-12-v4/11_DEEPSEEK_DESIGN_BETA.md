# ONE-SHOT WORK HANDOFF — DeepSeek V4.1 Flash Design Beta

## Model routing
- MAIN evaluator/product owner: **GPT-6 Astra — High; XHigh for final model-placement decision**
- Independent visual benchmark judge: **GPT-6 Astra — XHigh**
- Benchmark harness/integration implementation: **GPT-5.6 Sol — High**
- Dataset/scoring/repeatability worker: **GPT-5.6 Terra — Medium**
- Current evidence/repository/benchmark research: **GPT-5.6 Luna — Medium**
- Never use Pro in Work.

Take ownership of the effort to determine whether DeepSeek V4.1 Flash has a real production advantage in design and, if so, turn that advantage into a minimal useful beta.

Recover prior comparisons involving DeepSeek, GLM, Open Design Arena, UI/product design, visual composition, implementation quality, and other relevant models. Do not assume leaderboard rank equals production superiority.

Create discriminating tasks for UI composition, hierarchy, product screens, components/design systems, landing pages, iterative correction, screenshot critique where supported, design-system adherence, and implementation handoff.

Sol builds the harness/integration. Terra ensures repeatability and scoring discipline. Luna gathers external evidence. Astra is the independent visual judge and decides actual placement in the stack.

Classify where DeepSeek wins, loses, or is inconclusive. If the advantage is real, implement the smallest clean beta workflow and test it. Do not build infrastructure around a benchmark artifact. End with confidence level and continue/stop recommendation.