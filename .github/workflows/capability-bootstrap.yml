name: Apply LIN 3D capability gate

on:
  push:
    branches:
      - candidate/tool-capability-lin3d-gate-20260914
    paths:
      - '.capability-bootstrap/**'
      - '.github/workflows/capability-bootstrap.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: capability-bootstrap-${{ github.ref }}
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout isolated candidate
        uses: actions/checkout@v4
        with:
          ref: candidate/tool-capability-lin3d-gate-20260914
          fetch-depth: 0
      - name: Restore, validate, test, and commit final gate
        shell: bash
        run: bash .capability-bootstrap/apply.sh
